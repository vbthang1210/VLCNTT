{
  "version": 1,
  "recipe": {
    "name": "ESP32 Audio host stack",
    "kind": "composite",
    "bootstrap": ["uv sync --all-groups --no-install-project"],
    "build": [],
    "test": [
      "uv run --directory backend pytest -q",
      "npm --prefix frontend run check",
      "uv run --directory backend python -m compileall -q app tests",
      "arduino-cli compile --clean --warnings all --libraries C:\Users\vongb\Documents\Arduino\libraries --fqbn esp32:esp32:esp32 main"
    ],
    "start": "uv run --directory backend python run.py",
    "port": 8000,
    "readinessPath": "/health",
    "evidence": ["Backend Flask API in backend/", "Node frontend in frontend/", "Arduino sketch in main/", "Mosquitto is external on 127.0.0.1:1883"]
  }
}
